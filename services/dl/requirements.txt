# Module 2 — Deep Learning (computer vision) service.
-e ../../shared

# CPU wheels by default. For GPU, install the matching CUDA build of torch.
torch>=2.2
torchvision>=0.17
pillow>=10.0
numpy>=1.26,<2

fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9   # required for image file uploads
pydantic>=2.6
pydantic-settings>=2.2

pytest>=8.0
httpx>=0.27
